
<RULE[user_local]>
When creating or modifying HTML pages in this project, NEVER use inline styles or custom <style> blocks for layout and standard UI elements. Always use the predefined classes from style.css (e.g., .dashboard, .settings-panel, .control-grid, .control-item, .main-view, .main-controls) to maintain the Swiss Design System consistency.
</RULE[user_local]>
